<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>A Title</title>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css" integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  </head>
  <body>
    <div class="container">
      <div class="jumbotron centered">
        <h1 class="display-3">Form</h1>

        <form action="/findRoute" method="POST">
          <p>1. Enter Start and destination coordinates in the format lat,long without spaces</p>
          <div class="form-group">
            <input type="text" class="form-control text-center" name="startpos" placeholder="Start Coordinates">
            <input type="text" class="form-control text-center" name="endpos" placeholder="Destination Coordinates">
          </div>
          <button type="submit" class="btn btn-dark">Submit</button>
        </form>

        <br><br>

        <form action="/services" method="POST">
          <p>2. Services around a coordinate</p>
          <div class="form-group">
            <input type="text" class="form-control text-center" name="center" placeholder="Center coordinates">
            <input type="text" class="form-control text-center" name="service" placeholder="Service Name">
            <input type="text" class="form-control text-center" name="radius" placeholder="Radius in Km">
          </div>
          <button type="submit" class="btn btn-dark">Show on Map</button>
        </form>

        <br> <br>

        <form action="/map" method="POST">
          <p>3. Click the below to choose points on the map and save them in the database</p>
          <button type="submit" class="btn btn-dark">Open Map</button>
        </form>
        <form action="/showPoints" method="post">
          <button type="submit" class="btn btn-dark">Show Saved Points</button>
        </form>

        <br><br>

        <form action="/liveLocation" method="POST">
          <p>4. Click the below to show your live location on the map</p>
          <button type="submit" class="btn btn-dark">Open Map</button>
        </form>
      </div>
    </div>
  </body>
  <!-- <script>
    let constraintObj = {
      audio: true,
      video: false
    };

    <p>4. Audio</p>
    <p>
      <button id="btnStart">Start Recording</button>
      <button id="btnEnd">Stop Recording</button>
    </p>
    <audio id="aud" controls></audio>

    navigator.mediaDevices.getUserMedia(constraintObj).then(function(mediaStreamObj){
      let start = document.getElementById('btnStart');
      let stop = document.getElementById('btnEnd');
      let savedFile = document.getElementById('aud');
      let mediaRecorder = new MediaRecorder(mediaStreamObj);
      let chunks = [];

      start.addEventListener("click", function(ev){
        mediaRecorder.start();
        console.log(mediaRecorder.state);
      });
      stop.addEventListener("click", function(ev){
        mediaRecorder.stop();
        console.log(mediaRecorder.state);
      });

      mediaRecorder.ondataavailable = function(ev){chunks.push(ev.data);}
      mediaRecorder.onstop = function(ev){
        let blob = new Blob(chunks, {type: 'audio/*;'});
        chunks = [];
        let audioUrl = window.URL.createObjectURL(blob);
        savedFile.src = audioUrl;
      }
    }).catch(function(err){
      console.log(err);
    })
  </script> -->
</html>
